# 改动记录

按版本倒序。只记「改了什么、怎么改的」，详细说明见 [Release notes](docs/)。

---

## v0.3.0 —— 一命挑战模式

**新增**

- 一命挑战模式：拿起金草莓 / 银草莓时整块面板镀色，并切换成只显示这一命的数据
- 本面近 20 次通过情况（走势条）：20 个方块，绿=通过 / 红=失败 / 空心=无记录
- 带金成功率、进入率、带金死亡、连续通过 / 最高连续通过
- 模糊层重做：从「逐张卡片加 blur」改成「统一遮罩」，边缘羽化

**修复**

- 本面死亡数不实时 —— `renderRoomInfo` 的提前返回把它挡掉了
- 带银显示成带金 —— `goldenType` 映射反了（`0 = 金`、`1 = 银`）
- 进入率取错字段 —— 应为 `{room:goldenEntryChance}`，原用了 `{room:chokeRate}`
- 走势条不刷新 —— CCT 的「暂停死亡追踪」开启时不记录房间尝试
- 重开章节后死亡数残留 —— 重开后第一次检测只记基准、不计数
- 第二张卡片空白 —— 走势条没数据时整行被隐藏
- 方格只有进入动画没有退出动画 —— 从重建节点改成复用节点
- 跨天丢数据 —— 存档从 `sessionStorage` 换成 `localStorage`
- 银卡退场变金、切换动画被取消、镀色方向反了等一批动画问题

**动画**

- 镀色扫光：从左边缘往右扫上，死亡退出时往右扫走
- 成功率的数值缓动（先快后慢），进入率 / 局数保留弹跳
- 走势方格变化时从右往左依次过渡

---

## v0.2.0 —— 计时修复与动画

**修复**

- 暂停时长没有计入上一面用时
- 换图后总用时虚高（把暂停时间也算进去了）
- 切图后当前房间的时间丢失
- 「重新开始此章节」不清零
- 返回地图选不保存进度时不清零
- 从后面小节进入时不清零

**新增**

- 右侧进度条 + 进度百分比（数值平滑缓动）
- 平均死亡 / 平均用时
- 页面内调试面板（`DEBUG` 开关控制）

**动画**

- 三张卡片依次淡入上浮
- 当前房间持续呼吸光晕、方格被攻克时短暂高亮
- 死亡时顶部卡片短暂闪红
- 攻下整个小节时卡片边缘掠过绿光

---

## v0.1.0 —— 首个版本

- 覆盖层基础：全局信息（地图名 / 房间名 / 总死亡 / 总用时）、本面信息（本面死亡 / 本面用时）、进度方格
- 过图时定格上一面数据并变色，再用「先快后慢」的缓动过渡
- `LevelWatcher` 辅助 Mod：暴露当前场景类型，用于判断玩家是否在关卡内
